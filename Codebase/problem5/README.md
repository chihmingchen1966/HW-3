
# Homework 3 — Problem 5

This folder contains my solution for **Homework 3, Problem 5**.

## Overview

Problem 5 focuses on implementing and running the computational experiment required for the final part of Homework 3.

The purpose of this folder is to provide a reproducible codebase for the Problem 5 experiment. The script is used to generate the numerical results, output files, and figures discussed in the corresponding final PDF report.

The implementation is organized to:

* run the Problem 5 main experiment,
* generate the numerical output used in the report,
* save experiment results for later inspection,
* produce figures or data files when required,
* make the experiment reproducible with a fixed random seed,
* allow the grader to verify the result directly from the submitted code.

## Files

```text
problem5/
├── README.md
├── problem5_main.py
└── problem5_outputs/
```

### `problem5_main.py`

Main Python script for Problem 5.

This script contains the implementation used to generate the Problem 5 experimental results reported in the HW3 Problem 5 final report.

Depending on the assignment requirement, the script may include:

* problem initialization,
* construction of the target Hamiltonian, matrix, model, or objective function,
* execution of the main algorithm,
* sampling or subspace-based computation,
* numerical evaluation of the result,
* saving of result tables,
* generation of output plots.

### `problem5_outputs/`

Output folder for files generated by `problem5_main.py`.

This folder may contain result files such as:

* CSV result tables,
* numerical summary files,
* error analysis output,
* figures generated from the experiment.

Example outputs may include:

```text
problem5_outputs/
├── problem5_results.csv
├── problem5_summary.csv
├── problem5_error_vs_shots.png
└── problem5_error_vs_subspace_dimension.png
```

The exact output files depend on the final version of the script.

### `README.md`

This file.

It explains the purpose of the folder, the expected file structure, and the command used to run the code.

## How to Run

From the root directory of the repository, run:

```bash
python3 Codebase/problem5/problem5_main.py --seed 10010022 --outdir Codebase/problem5/problem5_outputs
```

Or, if you are already inside this folder:

```bash
python3 problem5_main.py --seed 10010022 --outdir problem5_outputs
```

## Expected Output

After running the script, the terminal should display the main numerical result for Problem 5.

The script may also generate output files inside the `problem5_outputs/` folder.

The expected outputs are intended to correspond to the numerical result, tables, figures, and discussion presented in the HW3 Problem 5 final report.

## Reproducibility

The experiment is designed to be reproducible.

The random seed used for the submitted result is:

```text
10010022
```

Using the same seed and command should allow the grader to reproduce the reported output as closely as possible.

## Notes for Grading

* The main entry point is `problem5_main.py`.
* The recommended execution command is provided in the **How to Run** section.
* The generated output files are saved under `problem5_outputs/`.
* The final numerical result should match the result discussed in the HW3 Problem 5 PDF report.
* This folder is organized independently so that Problem 5 can be checked separately from the other homework problems.

## Relationship to the Report

The Python script provides the computational experiment and generated results.

The corresponding HW3 Problem 5 final PDF report provides:

* the problem background,
* the method explanation,
* the experiment setup,
* the numerical results,
* the generated figures or tables,
* and the final interpretation of the result.

Therefore, this folder should be read together with the HW3 Problem 5 final report PDF.

## Summary

In short, this folder provides the complete reproducible code and output structure for Homework 3, Problem 5.
The code is intended to support the final report by allowing the reported result, CSV files, and figures to be regenerated from the submitted script.
